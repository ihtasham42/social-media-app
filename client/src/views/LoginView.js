import { Card, TextField } from "@mui/material";
import React from "react";

const LoginView = () => {
  return (
    <Card sx={{ mt: 5 }}>
      <TextField />
      <TextField />
    </Card>
  );
};

export default LoginView;
